{
  "name": "fastdas-growth-engine-demo",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/patch-legacy-assets.mjs",
    "lint": "eslint .",
    "verify:demo": "node scripts/verify-demo.mjs",
    "verify:ui": "node scripts/verify-ui.mjs",
    "verify:prod-smoke": "node scripts/prod-smoke.mjs",
    "verify:all": "npm run lint && npm run build && npm audit && npm run verify:demo && npm run verify:ui",
    "preview": "vite preview --host 0.0.0.0",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist --project-name fastdas-growth-engine-demo --branch main"
  },
  "dependencies": {
    "control-surface-ui": "file:vendor/control-surface-ui-0.1.0.tgz",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitejs/plugin-react": "^6.0.2",
    "eslint": "^10.4.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.6.0",
    "playwright-core": "^1.61.0",
    "vite": "^8.0.16"
  },
  "overrides": {
    "esbuild": "0.28.1",
    "ws": "8.21.0"
  }
}
